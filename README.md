<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Washington Parcel Utility Finder</title>
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<style>
:root{--ink:#17221b;--muted:#657168;--bg:#f4f6f3;--card:#fff;--accent:#23633f;--blue:#245f86;--border:#d9e0da;--warn:#fff4d6}
*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,sans-serif;background:var(--bg);color:var(--ink)}
header{padding:24px max(20px,5vw);background:#173f2a;color:#fff}header h1{margin:0 0 6px;font-size:clamp(24px,4vw,38px)}header p{margin:0;color:#d7e6dc}
main{max-width:1250px;margin:0 auto;padding:22px;display:grid;grid-template-columns:390px 1fr;gap:18px}.panel,.map-wrap{background:var(--card);border:1px solid var(--border);border-radius:14px;box-shadow:0 3px 14px #0000000a}.panel{padding:18px}.field{margin-bottom:14px}label{display:block;font-weight:700;margin-bottom:6px}input,select,button{width:100%;padding:11px 12px;border-radius:9px;border:1px solid #bfc9c1;font:inherit}button{background:var(--accent);color:#fff;border:0;font-weight:800;cursor:pointer}button:disabled{opacity:.6;cursor:wait}.tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:15px 0}.tab{background:#e8eeea;color:#234334}.tab.active{background:var(--accent);color:#fff}
.hint,.fine{font-size:12px;color:var(--muted);line-height:1.45}.status{margin:14px 0;padding:10px;border-radius:9px;background:#eef3ef}.error{background:#fde8e6;color:#86251c}.warning{background:var(--warn)}#map{height:650px;border-radius:14px}.results{margin-top:16px}.item{border-top:1px solid var(--border);padding:13px 0}.item:first-child{border-top:0}.item h3{margin:0 0 7px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;font-size:14px}.grid span:nth-child(odd){color:var(--muted)}a{color:#155f82}.badge{display:inline-block;font-size:11px;font-weight:800;border-radius:999px;padding:3px 7px;background:#e5edf3;color:#245f86;margin-left:5px}.notice{padding:10px;border-radius:9px;background:var(--warn);font-size:13px;line-height:1.45}.hidden{display:none}
@media(max-width:850px){main{grid-template-columns:1fr}.panel{order:1}.map-wrap{order:2}#map{height:450px}}
</style></head>
<body><header><h1>Washington Parcel Utility Finder</h1><p>Find nearby recorded wells, likely electric utility territory, and publicly mapped power infrastructure.</p></header>
<main><section class="panel">
<form id="searchForm"><div class="field"><label for="county">County</label><select id="county" required></select></div><div class="field"><label for="parcel">Parcel ID</label><input id="parcel" required placeholder="Example: 12345.6789" autocomplete="off"><div class="hint">Enter the county assessor parcel number. Spaces, dashes and periods are ignored.</div></div><button id="searchBtn" type="submit">Analyze parcel</button></form>
<div id="status" class="status">Ready.</div><div id="parcelInfo"></div>
<div id="tabs" class="tabs hidden"><button class="tab active" data-tab="wells" type="button">Water wells</button><button class="tab" data-tab="power" type="button">Power</button></div>
<div id="wellResults" class="results"></div><div id="powerResults" class="results hidden"></div>
<p class="fine">This is preliminary screening data. Well points can be approximate. Power infrastructure comes from public OpenStreetMap mapping and may be incomplete. Always confirm electric service, capacity, transformer needs and extension pricing directly with the utility.</p>
</section><section class="map-wrap"><div id="map"></div></section></main>
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script><script src="https://cdn.jsdelivr.net/npm/@turf/turf@7/turf.min.js"></script>
<script>
const PARCEL_URL='https://services.arcgis.com/jsIt88o09Q0r1j8h/arcgis/rest/services/Current_Parcels/FeatureServer/0/query';
const WELL_URL='https://services.arcgis.com/6lCKYNJLvwTXqrmp/ArcGIS/rest/services/WR/FeatureServer/9/query';
const UTILITY_URL='https://gis.ecology.wa.gov/serverext/rest/services/CPR/CPR/MapServer/0/query';
const OVERPASS=['https://overpass-api.de/api/interpreter','https://overpass.kumi.systems/api/interpreter'];
const countyCodes={Adams:'001',Asotin:'003',Benton:'005',Chelan:'007',Clallam:'009',Clark:'011',Columbia:'013',Cowlitz:'015',Douglas:'017',Ferry:'019',Franklin:'021',Garfield:'023',Grant:'025','Grays Harbor':'027',Island:'029',Jefferson:'031',King:'033',Kitsap:'035',Kittitas:'037',Klickitat:'039',Lewis:'041',Lincoln:'043',Mason:'045',Okanogan:'047',Pacific:'049','Pend Oreille':'051',Pierce:'053','San Juan':'055',Skagit:'057',Skamania:'059',Snohomish:'061',Spokane:'063',Stevens:'065',Thurston:'067',Wahkiakum:'069','Walla Walla':'071',Whatcom:'073',Whitman:'075',Yakima:'077'};
const countyEl=document.getElementById('county');Object.keys(countyCodes).forEach(c=>countyEl.add(new Option(c,c)));countyEl.value='Spokane';
const map=L.map('map').setView([47.4,-120.7],7);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);
let displayLayer=L.featureGroup().addTo(map),last={parcel:null,wells:[],power:null},activeTab='wells';
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function norm(s){return String(s||'').toUpperCase().replace(/[^A-Z0-9]/g,'')}function sql(s){return String(s).replaceAll("'","''")}
async function arcQuery(base,params){const u=new URL(base);Object.entries(params).forEach(([k,v])=>u.searchParams.set(k,v));const r=await fetch(u);if(!r.ok)throw new Error(`Data service returned ${r.status}`);const j=await r.json();if(j.error)throw new Error(j.error.message||'ArcGIS query failed');return j}
function dateFmt(ms){return ms?new Date(ms).toLocaleDateString():'Not reported'}function val(v,s=''){return v===null||v===undefined||v===''?'Not reported':`${esc(v)}${s}`}
function distText(mi){const ft=mi*5280;return ft<5280?`${ft.toFixed(0)} ft`:`${mi.toFixed(2)} mi`}
async function findParcel(county,input){const raw=input.trim(),upper=sql(raw.toUpperCase()),n=norm(raw),fips=countyCodes[county],shortFips=String(Number(fips));const fields='FIPS_NR,COUNTY_NM,ORIG_PARCEL_ID,PARCEL_ID_NR,SITUS_ADDRESS,SITUS_CITY_NM,SITUS_ZIP_NR,DATA_LINK';const countyWhere=`(FIPS_NR='${fips}' OR FIPS_NR='${shortFips}' OR COUNTY_NM='${shortFips}' OR COUNTY_NM='${fips}')`;const exactWhere=`${countyWhere} AND (UPPER(ORIG_PARCEL_ID)='${upper}' OR UPPER(PARCEL_ID_NR)='${upper}' OR UPPER(PARCEL_ID_NR)='${fips}${upper}')`;let d=await arcQuery(PARCEL_URL,{f:'geojson',where:exactWhere,outFields:fields,returnGeometry:'true',outSR:'4326',resultRecordCount:'20'});if(!d.features.length){const likeWhere=`${countyWhere} AND (UPPER(ORIG_PARCEL_ID) LIKE '%${sql(n)}%' OR UPPER(PARCEL_ID_NR) LIKE '%${sql(n)}%')`;d=await arcQuery(PARCEL_URL,{f:'geojson',where:likeWhere,outFields:fields,returnGeometry:'true',outSR:'4326',resultRecordCount:'100'});d.features=d.features.filter(f=>{const o=norm(f.properties.ORIG_PARCEL_ID),s=norm(f.properties.PARCEL_ID_NR);return o===n||s===n||s===norm(fips+n)||s.endsWith(n)})}if(d.features.length!==1)throw new Error(d.features.length?'Multiple parcel matches found. Try the exact assessor parcel format.':'Parcel not found. Confirm the county and assessor parcel number.');d.features[0].properties._countyDisplay=county;return d.features[0]}
async function findWells(parcel){const [lon,lat]=turf.centroid(parcel).geometry.coordinates;let miles=2,features=[];while(miles<=50&&features.length<3){const latPad=miles/69,lonPad=miles/(69*Math.cos(lat*Math.PI/180)),env={xmin:lon-lonPad,ymin:lat-latPad,xmax:lon+lonPad,ymax:lat+latPad,spatialReference:{wkid:4326}};const d=await arcQuery(WELL_URL,{f:'geojson',where:"WellProjectType <> 'Decommission'",geometry:JSON.stringify(env),geometryType:'esriGeometryEnvelope',inSR:'4326',spatialRel:'esriSpatialRelIntersects',outSR:'4326',returnGeometry:'true',outFields:'ID,WellTagNr,ProjectName,FileName,CompletedDepth,WorkCompletionDate,WellProjectType,WellSubType,StaticWaterLvl,FlowRateGPM',resultRecordCount:'2000'});features=d.features;miles*=2}const b=turf.polygonToLine(parcel);features.forEach(f=>f.properties._distance=turf.pointToLineDistance(f,b,{units:'miles'}));return features.sort((a,b)=>a.properties._distance-b.properties._distance).slice(0,3)}
async function findUtility(parcel){const [x,y]=turf.centroid(parcel).geometry.coordinates;try{const d=await arcQuery(UTILITY_URL,{f:'json',where:'1=1',geometry:`${x},${y}`,geometryType:'esriGeometryPoint',inSR:'4326',spatialRel:'esriSpatialRelIntersects',outFields:'Name',returnGeometry:'false'});return d.features?.map(f=>f.attributes.Name).filter(Boolean)||[]}catch(e){return []}}
function bboxFor(parcel,miles){const [lon,lat]=turf.centroid(parcel).geometry.coordinates,dy=miles/69,dx=miles/(69*Math.cos(lat*Math.PI/180));return [lat-dy,lon-dx,lat+dy,lon+dx]}
async function overpassQuery(q){let err;for(const url of OVERPASS){try{const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded;charset=UTF-8'},body:'data='+encodeURIComponent(q)});if(!r.ok)throw new Error(`Overpass ${r.status}`);return await r.json()}catch(e){err=e}}throw err||new Error('Public power-map service unavailable')}
function osmToFeatures(data){const out=[];for(const e of data.elements||[]){const tags=e.tags||{},kind=tags.power||'unknown';if(e.type==='node'&&Number.isFinite(e.lat)&&Number.isFinite(e.lon)){out.push(turf.point([e.lon,e.lat],{...tags,_osmType:e.type,_osmId:e.id,_kind:kind}))}else if(e.type==='way'&&Array.isArray(e.geometry)&&e.geometry.length>1){out.push(turf.lineString(e.geometry.map(p=>[p.lon,p.lat]),{...tags,_osmType:e.type,_osmId:e.id,_kind:kind}))}}return out}
async function findPower(parcel){let miles=3,features=[];while(miles<=25&&features.length<3){const [s,w,n,e]=bboxFor(parcel,miles);const q=`[out:json][timeout:30];(way[power~"^(line|minor_line|cable)$"](${s},${w},${n},${e});node[power~"^(pole|tower|transformer)$"](${s},${w},${n},${e}););out tags geom;`;features=osmToFeatures(await overpassQuery(q));miles=miles<6?6:miles<12?12:25}const boundary=turf.polygonToLine(parcel);features.forEach(f=>{f.properties._distance=f.geometry.type==='Point'?turf.pointToLineDistance(f,boundary,{units:'miles'}):Math.min(turf.pointToLineDistance(turf.pointOnFeature(f),boundary,{units:'miles'}),turf.distance(turf.nearestPointOnLine(f,turf.centroid(parcel)),turf.centroid(parcel),{units:'miles'})))});const lines=features.filter(f=>f.geometry.type==='LineString').sort((a,b)=>a.properties._distance-b.properties._distance).slice(0,3),assets=features.filter(f=>f.geometry.type==='Point').sort((a,b)=>a.properties._distance-b.properties._distance).slice(0,3);return {utility:await findUtility(parcel),lines,assets}}
function lineClass(p){if(p._kind==='minor_line')return 'Distribution/minor line';if(p._kind==='line')return p.voltage?`Power line (${p.voltage} V listed)`:'Power line—possibly transmission';if(p._kind==='cable')return 'Underground cable';return p._kind}
function renderMap(){displayLayer.clearLayers();if(!last.parcel)return;L.geoJSON(last.parcel,{style:{color:'#23633f',weight:3,fillOpacity:.12}}).addTo(displayLayer);if(activeTab==='wells')last.wells.forEach((w,i)=>L.geoJSON(w,{pointToLayer:(f,ll)=>L.marker(ll).bindPopup(`<b>Well ${i+1}</b><br>${distText(w.properties._distance)}`)}).addTo(displayLayer));else if(last.power){last.power.lines.forEach((f,i)=>L.geoJSON(f,{style:{color:'#d27b00',weight:4}}).bindPopup(`<b>Power line ${i+1}</b><br>${distText(f.properties._distance)}`).addTo(displayLayer));last.power.assets.forEach((f,i)=>L.geoJSON(f,{pointToLayer:(x,ll)=>L.circleMarker(ll,{radius:7,weight:2,fillOpacity:.8}).bindPopup(`<b>${esc(f.properties._kind)}</b><br>${distText(f.properties._distance)}`)}).addTo(displayLayer))}if(displayLayer.getLayers().length)map.fitBounds(displayLayer.getBounds().pad(.25))}
function renderParcel(p){$('parcelInfo').innerHTML=`<h2>Parcel</h2><div class="grid"><span>County</span><b>${esc(p._countyDisplay||p.COUNTY_NM)}</b><span>Parcel ID</span><b>${esc(p.ORIG_PARCEL_ID||p.PARCEL_ID_NR)}</b><span>Address</span><b>${esc([p.SITUS_ADDRESS,p.SITUS_CITY_NM,p.SITUS_ZIP_NR].filter(Boolean).join(', ')||'Not listed')}</b></div>`}
function renderWells(wells){$('wellResults').innerHTML='<h2>Closest recorded wells</h2>'+wells.map((w,i)=>{const a=w.properties;return `<article class="item"><h3>${i+1}. ${esc(a.WellTagNr||a.ProjectName||'Well report')}</h3><div class="grid"><span>Distance</span><b>${distText(a._distance)}</b><span>Completed depth</span><b>${val(a.CompletedDepth,' ft')}</b><span>Static water level</span><b>${val(a.StaticWaterLvl,' ft')}</b><span>Reported yield</span><b>${val(a.FlowRateGPM,' GPM')}</b><span>Completion date</span><b>${dateFmt(a.WorkCompletionDate)}</b><span>Type</span><b>${esc([a.WellProjectType,a.WellSubType].filter(Boolean).join(' / ')||'Not reported')}</b></div></article>`}).join('')}
function renderPower(p){const utility=p.utility.length?p.utility.join(' / '):'Not identified from public territory layer';let html=`<h2>Power screening</h2><div class="notice"><b>Likely serving utility:</b> ${esc(utility)}<br>Territory boundaries are approximate. Confirm directly with the utility before relying on this result.</div>`;html+='<h3>Closest mapped power lines</h3>';html+=p.lines.length?p.lines.map((f,i)=>{const a=f.properties;return `<article class="item"><h3>${i+1}. ${esc(lineClass(a))}<span class="badge">OSM</span></h3><div class="grid"><span>Distance</span><b>${distText(a._distance)}</b><span>Voltage</span><b>${val(a.voltage,' V')}</b><span>Cables</span><b>${val(a.cables)}</b><span>Operator</span><b>${val(a.operator)}</b></div></article>`}).join(''):'<p>No publicly mapped lines found within the search area.</p>';html+='<h3>Closest mapped poles, towers or transformers</h3>';html+=p.assets.length?p.assets.map((f,i)=>{const a=f.properties;return `<article class="item"><h3>${i+1}. ${esc(a._kind)}<span class="badge">OSM</span></h3><div class="grid"><span>Distance</span><b>${distText(a._distance)}</b><span>Operator</span><b>${val(a.operator)}</b><span>Reference</span><b>${val(a.ref)}</b></div></article>`}).join(''):'<p>No publicly mapped poles, towers or transformers found nearby.</p>';html+='<div class="notice"><b>Interpret carefully:</b> a nearby line may be transmission-only, privately owned, across a road, at capacity, or lack a usable transformer. This result estimates proximity—not service availability or construction cost.</div>';$('powerResults').innerHTML=html}
function switchTab(tab){activeTab=tab;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));$('wellResults').classList.toggle('hidden',tab!=='wells');$('powerResults').classList.toggle('hidden',tab!=='power');renderMap()}
document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.tab)));
$('searchForm').addEventListener('submit',async e=>{e.preventDefault();const btn=$('searchBtn');btn.disabled=true;$('tabs').classList.add('hidden');$('wellResults').innerHTML='';$('powerResults').innerHTML='';$('parcelInfo').innerHTML='';$('status').className='status';$('status').textContent='Finding parcel…';try{const parcel=await findParcel(countyEl.value,$('parcel').value);last={parcel,wells:[],power:null};renderParcel(parcel.properties);$('status').textContent='Parcel found. Searching wells and public power data…';const results=await Promise.allSettled([findWells(parcel),findPower(parcel)]);last.wells=results[0].status==='fulfilled'?results[0].value:[];last.power=results[1].status==='fulfilled'?results[1].value:{utility:await findUtility(parcel),lines:[],assets:[]};renderWells(last.wells);renderPower(last.power);$('tabs').classList.remove('hidden');switchTab('wells');$('status').className='status warning';const powerNote=results[1].status==='rejected'?' Public power infrastructure lookup was unavailable; try again later.':'';$('status').textContent=`Parcel analyzed. Found ${last.wells.length} nearby well report${last.wells.length===1?'':'s'} and ${last.power.lines.length} mapped power line${last.power.lines.length===1?'':'s'}.${powerNote}`;}catch(err){$('status').className='status error';$('status').textContent=err.message}finally{btn.disabled=false}});
</script></body></html>
